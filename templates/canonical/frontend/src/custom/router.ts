// custom/router — extension point for `router/index.ts`. Routes added
// here are appended after Blast's generated routes. Use this for
// app-specific landing pages, auth screens, marketing pages, etc.

import type { RouteRecordRaw } from 'vue-router'

export const customRoutes: Array<RouteRecordRaw> = []
